services:
  enricher:
    image: ghcr.io/alpenl/cairn-x-enricher:0.3.0
    container_name: cairn-x-enricher
    env_file:
      - .env
    environment:
      HTTP_ADDR: :8080
    ports:
      - "8088:8080"
    restart: unless-stopped
    init: true
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:mode=1777
    stop_grace_period: 20s
    healthcheck:
      test: ["CMD", "/cairn-x-enricher", "healthcheck"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
