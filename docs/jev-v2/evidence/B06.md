# B06 证据：Web 字段级整理、真实依据与独立分类状态

报告日期：2026-09-20
Batch / Issue：B06 / [E #13](https://github.com/Alpenl/cairn-x-enricher/issues/13)
Covered task IDs / scenario IDs：B06-T01、T02、T03、T04（CAS/dirty）、T05（转义）、T08（部分）、T11、T12；SC03、SC04、SC10、SC14、SC15、SC28（部分）
Implementation branch：`impl/jev-v2-b02-runtime-safety`
Base / Companion SHA：E base `cb13d0e543e54f18baf6e151630d728b3175e792`；S companion B05 分支
设计/spec 版本：`docs/jev-v2/06-curation-ui.md`

## 变更

- **B06-T01 Go 同源代理/DTO**：`internal/cairn/v2.go` 新增 `V2Selection`、
  `V2SelectionView`、`V2Taxonomy`、`V2Override` 与 `GetV2Selection`/`UpdateV2Selection`/
  `GetV2Taxonomy`/`ApplyV2Override`/`GetV2Effective`。`ErrV2Unsupported` 明确区分“后端不支持”
  与“选择为空”：dashboard 路由在 backend 不实现 `V2Backend` 时返回
  `{"available":false,"reason":"v2_unsupported"}`，UI 降级隐藏而非清空数据。
  `internal/dashboard/dashboard.go` 新增 `/api/bookmarks/{id}/v2-selection`（GET/PATCH）、
  `/api/v2-taxonomy`、`/api/bookmarks/{id}/v2-override`、`/api/bookmarks/{id}/v2-effective`。
- **B06-T02 展示/编辑分离**：`internal/dashboard/curation-v2.js` + `reader.html` 新增
  `<details id="v2-curation">` 面板，分维度渲染 topics/content_functions/carriers/affordances；
  默认不展示概率；超过三个主题折叠并显示“另外 N 个仍然保留”，第四个主题仍可勾选；
  inactive/历史标签仍可见且标注“（停用）”。
- **B06-T03 字段级操作**：`accept`/`reject`/`set_empty`/`reset` 各自独立控制；payload 带
  `operation_key` 与 `expected_revision`；只提交被触发的字段。
- **B06-T04 CAS 与 dirty**：dirty 状态绑定表单；`revision_conflict` 时提示实际差异并重新加载，
  不无条件 last-write-win；失败保留草稿；操作 key 幂等。
- **B06-T05 依据与安全渲染**：原文经 `ui.element`/`textContent` 渲染；浏览器测试断言
  `<script>alert(1)</script>` 不会变成 HTML 或执行，且无 page error。
- **B06-T08 部分**：v2 面板按 capability 显示；mock 交互覆盖筛选/请求无旧覆盖。
- **B06-T11 可访问与移动端**：`aria-live` 状态区、键盘可聚焦控件、长标签 `overflow-wrap:anywhere`、
  `prefers-reduced-motion` 与 375px 断点；浏览器测试断言 375px 无横向溢出。
- **B06-T12 真实浏览器验收**：新增 `tests/browser/run.mjs`（Playwright + 系统 Chrome + 内置
  mock Worker，服务**真实** dashboard 资产）与 `make test-browser`。21 项断言全部检查**实际网络
  请求体**与调用计数，不只看截图。

## 回归与门禁

| 命令 | 结果 |
|---|---|
| `make verify`（含 lint-ci/test/test-frontend/build） | 退出码 0 |
| `make test-browser`（真实 Chrome，21 项） | 21/21 通过 |
| `node internal/dashboard/frontend-check.mjs` | 73/73 通过 |
| S 侧 `npm test` | 92/92 通过 |

浏览器验收覆盖：四主题完整且折叠；content_functions 多选与 carrier 独立；reject 发送带
operation key + expected_revision 的 override；set_empty 与 reset payload 不同；同 operation key
幂等重放；恶意原文被转义；why-only 保存不发 classification、不产生 override 事件；模型调用 0、
X Search 调用 0；键盘可聚焦；375px 无横向溢出。

- 模型调用计数：0（mock server；`state.modelCalls`/`state.xSearchCalls` 断言）。
- 回滚：UI v2 由 `available` 门控；后端不支持时隐藏面板，不清 v2 存储或人工事件。

## 限制和审批

- 未运行项 / blocked_external：
  - B06-T05 的 **block/span 级依据引用**为 partial：当前展示原文但未实现从 evidence_snapshots
    逐 block 渲染角色标签（原帖/续帖/引用/外链/第三方/legacy_unknown）。
  - B06-T06 独立分类状态（pending/processing/failed/exhausted、字段 abstained/not_applicable/stale）
    与后台错误汇总为 partial，未接入新状态机 UI。
  - B06-T07 “只重试分类 / 策略重算 / 刷新原文”三个动作在 CLI 已实现（B04），Web 按钮与 409 解释
    为 partial。
  - B06-T09 导出完整 v2 字段与 partial 计数为 partial。
  - B06-T10 实体视图与扩展入口 flag 为 partial。
  - B06-T13（规格中无此项）不适用。
- engineering_done：部分（T01–T05、T08 部分、T11、T12 有代码与真实浏览器测试）
- quality_verified：是（离线 + 真实浏览器）；无 live 模型
- review_accepted / merged / deployment_authorized：否
- 实际生产操作：无
- 风险/设计偏离：v2 面板与旧 v1 表单并存，未替换旧交互（spec 允许渐进）；无其他偏离。
- 后续审查要求：补齐依据 block 渲染、独立分类状态与三动作按钮；勿以本文件自动关闭 E #13。
