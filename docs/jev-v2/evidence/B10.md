> 2026-09-23 NAS 图片兼容修复：[私有缓存、旧后端孤儿读取与真实浏览器](B10-20260923-nas-image.md)。E `86be050` / S `c2091fa`；Go 取图前后检查所属收藏，NAS no-store，生产 URL 避开旧缓存键。完整 make verify、13 实际服务、12 缓存浏览器及 40 完整浏览器检查通过；旧夹具 policy-v2 等待失败与修正记录保留。无新增付费，累计 211 次；完整原范围与独立复审继续。

> 2026-09-23 Android 删除修复：[持久意图、队列清理与迟到响应/图片缓存](B07-20260923-delete.md)。S `c2091fa`；最终源码 CI 与 API26/35 均通过，各 35 普通测试 + 13 个真实 Worker 独立进程阶段；73 unit、lint/strict dependency/build 通过。旧实现详情复活失败对照与完整原始日志保留。无新付费，累计 211 次；旧 Worker 兼容、NAS 缓存、完整保留期及其余原范围继续。

> 2026-09-23 隐私删除修复：[真实 HTTP、D1 全关联表与 R2 恢复](B10-20260923-privacy.md)。S `7b0aa9b` / E `06e56ad`；0026 持久删除凭据、预算清理与缓存失效，图片读写删除竞争守卫，定时重试和旧孤儿发现。221 Worker、typecheck/dry-run、完整 Go 门禁、12 个实际服务及最终生命周期复验通过。无新付费，累计 211 次；生产未迁移/部署，客户端缓存及完整保留期范围继续。

# B10 证据：跨仓库验收、迁移回滚与最终复审证据

报告日期：2026-09-20
Batch / Issue：B10 / [E #16](https://github.com/Alpenl/cairn-x-enricher/issues/16)
最终复审：[FINAL-REVIEW.md](FINAL-REVIEW.md)
部署 runbook（未执行）：[runbook-deploy.md](runbook-deploy.md)
两仓 SHA：E `e6956c6163457c413a7f1a0b78719807c0ee71ca`（`impl/jev-v2-b02-runtime-safety`）；
S `667625cdcb915d158f070efe1b9a9a798f865442`（`impl/jev-v2-b01-authoritative-target`）

## 变更

- **B10-T02/T12 追踪矩阵与复审**：新增 `docs/jev-v2/FINAL-REVIEW.md`，含基线/最终 SHA、
  R01–R39 覆盖表、SC01–SC30 逐项结果、126 任务映射到批次与证据、P0 失败→通过、调用计数、
  门禁、flags 与回滚、风险与偏离。
- **B10-T04 故障/并发集成**：新增 `internal/cairn/integration_test.go`（9 项）：响应丢失后
  按 operation key 重放不重复推断；stale 结果不暂停组件；capability mismatch 暂停组件且不
  领取任务；并发 claim 只授予一个 lease；typed 错误映射穷尽；legacy 无 key 提交仍可用。
- **B10-T06/T07 迁移/隐私集成**：新增 `worker/test/integration.test.ts`（9 项）：全部迁移表
  存在；legacy target 种子；v1 写入不抹 v2 与第四 topic；reading 失败保留 source；删除级联
  覆盖全部私人 v2 表；证据 payload 不含 note；超大/非法 role 证据被拒；SQL 注入 term 被拒；
  budget ledger 去重。
- **B10-T09 文档同步**：更新 `README.md`（Evaluate/Decide/Resolve、权威目标、扩展 flag、
  replay/refresh-source、浏览器测试、离线评估命令）与 `docs/architecture.md`（组件表含
  `extension` 与 `experiments/classification`）。
- **B10-T10 runbook**：`docs/jev-v2/runbook-deploy.md`（仅准备，不执行）。

## 回归与门禁

| 命令 | 结果 |
| --- | --- |
| E `make verify` | 退出码 0（lint + lint-ci + test + test-frontend + build） |
| E `make test-ablation` | 退出码 0 |
| E `make test-browser` | 21/21 通过 |
| E `go test ./...` | 全部包通过 |
| S `npm test` | 7 files / 101 tests 通过 |
| S `npm run typecheck` | 退出码 0 |
| S `npm run deploy:dry-run` | 退出码 0 |
| Android `testDebugUnitTest` | BUILD SUCCESSFUL |
| Android `--dependency-verification strict lintDebug assembleDebug compileDebugAndroidTestKotlin` | BUILD SUCCESSFUL |
| 空库迁移 0001–0012（sqlite3） | 无错误 |

## 最终不变量

| 类别 | 实证 |
| --- | --- |
| 权威目标 | 20 轮 A/B 不重复领取；generation 仅由受控 target 更新 |
| 一致性/幂等 | 过期 input/spec/target/lease 不覆盖；重复 key 不双写 |
| 人工主权 | why/status 不接受标签；reject/empty/reset 不同；v1 不抹隐藏字段 |
| 语义分层 | 多维分开；合法 none 成功；第四标签不丢 |
| 成本 | 阈值/display 调用 0；扩展有预算/降级；CI 零付费 |
| 证据真实性 | span 可回指；legacy 未知不编造；候选不凭空生成 |
| 协议 | 旧 shape 稳定；v1 写不抹 v2；strict 解码与 cache 表示正确 |
| 迁移 | 原文/人工保留；回滚不删历史；flag off 可用 |
| 质量诚实 | mock/gold 分开；样本不足 inconclusive |
| 隐私安全 | 删除清理完整；公开产物无私人内容；抓取不越界 |

## 限制和审批

- **未运行项 / blocked_external**：
  - B10-T03 完整 source-first 链路：以 mock Worker + 真实 client/processor 覆盖（无 Cloudflare
    网络依赖），未在真实 D1/R2 上跑端到端。
  - B10-T05 Android 设备/模拟器矩阵：`blocked_external`（无授权设备）；编译不代表执行。
  - B10-T08 真实质量报告：`blocked_external`（无人工 gold 与 live 授权）。
  - B10-T11 生产 D1/Worker 部署：未授权，未执行。
- engineering_done：是（T01/T02/T04/T06/T07/T09/T10/T11/T12 有代码/测试/文档；T03/T05/T08 部分）
- quality_verified：否（真实质量未验证）
- review_accepted / merged / deployment_authorized：否
- 实际生产操作：无（无远端迁移、无部署、无收费、无 merge/tag/force-push）
- 请求的下一步：独立复审，非自动合并。


## 2026-09-21 F01–F14 修复轮证据

实现：Enricher `impl/jev-v2-fixes-20260921` 代码 SHA `faad44c`（PR #17，上游固定审查 SHA `4031200984d819b59baea593025621a04deeb984`）；
Worker companion `4c03094`（PR Alpenl/cairn-share#32，上游固定审查 SHA `8f98ac0a9d6e5257840bd518cc5960464b74a98c`）。
本轮没有手工 seed 目标历史表：`tests/local-integration/run.sh` 从新收藏走真实链路生成记录。

| 命令 | 结果 |
|---|---|
| `make verify`（lint-ci + `go test -race` + 73/73 frontend checks + build） | exit 0 |
| `make test-ablation` | exit 0 |
| `make test-browser` | 29/29，exit 0 |
| `tests/local-integration/run.sh`（真实 Worker/D1/R2 + 真实 Go client/classifier） | PASS |
| Worker `npm test` / `npm run typecheck` / `npm run deploy:dry-run` | 107/107 / exit 0 / exit 0 |

真实组合证据：`tests/local-integration/run.sh` 启动 `wrangler dev`（真实 migrations、本地 D1/R2），
以真实 `cairn.Client` + 真实 `classify.Client` 完成 建收藏→SaveSource→SubmitEvidence→PutQuestionSpec→
激活 v2 target→handshake→claim→Evaluate（仅付费模型端点被官方合同 mock 替换）→complete→GetRuns→
DecodeStoredJudgments→Replay→ApplyV2Override→SubmitDecision→GetV2Selection，并断言人工 reject 在
replay 后仍生效。模型调用计数：付费调用 0；本地 mock 调用 1；replay 0。


### 2026-09-21 修复轮最终证据（替代旧报告的整体验收结论）
- 两仓测试代码 SHA：E `faad44c` / S `4c03094`；本轮报告文档提交 SHA 见 FINAL-REVIEW 更新提交。
- F01–F14 全部有实现与最低回归证据（见各批 evidence）；原 R01–R39 / 126 任务 / SC01–SC30 的
  整体验收状态按 FINAL-REVIEW 逐项重列，旧报告中的无条件“通过”已撤回为
  not_accepted / retest_required 或 partial。
- 真实本地集成（非 mockWorker）：`tests/local-integration/run.sh` 使用真实 Worker/D1/R2 +
  实际 Go client/classifier/HTTP 服务，PASS；付费模型 0 调用。
- 工程状态：F01–F14 engineering_done；quality_verified=false（无人工 gold/live 授权）；
  device 未运行；review_accepted=false；merged/deployed=false。
- 未做：合并、转 Ready、发版、远端迁移、生产部署、收费回填。



## 2026-09-21 第二轮：B06/B09 生产接入与真实浏览器端到端

实现 SHA：E `a85b870` / S `ccd7080`（同一 `impl/jev-v2-fixes-20260921` 分支，PR #17/#32）。

### 新增工程
- **B06-T05 依据**：Go 新增 `GET /api/bookmarks/{id}/evidence` 代理；`reader-v2-panels.js` 渲染真实 block 与角色（原帖/作者续帖/引用/外链/第三方/来源未知），缺失或截断显式标注，不生成似真解释。
- **B06-T06 独立状态**：`GET /api/bookmarks/{id}/classification-status` 代理，UI 分开展示 pending/processing/failed/exhausted/waiting_source 与原因。
- **B06-T07 三动作**：只重试分类（0 模型调用）、按当前策略重算（0 调用，dry-run 后显式写回）、重新抓取原文（有确认与成本说明），三者走不同后端行为。
- **B06-T09/B05-T13 导出**：`GET /api/export` 服务端 Markdown，含全部有效 v2 维度、why/来源、人工来源、过期/部分计数；首页按钮优先使用该端点，旧后端回退本地导出。
- **B09-T04 实体生命周期**：`internal/extension/service.go` 候选抽取 + 单次批量判断 + 预算 + 越界答案拒绝；状态 not_run/failed/completed_empty/completed_nonempty/stale；Worker `entity-state` 端点在事务守卫下写入，失败不清除同 revision 成功值。
- **B09-T05/T06 补证据**：processor 在分类成功后检测真实缺口（外链未取得/截断），创建 Worker evidence request（dedupe/预算），用受控 fetcher（allowlist + DNS/IP + 逐跳 redirect + MIME/大小/超时）抓取，成功则追加 external block、提交新快照并重新入分类队列；失败/被策略阻止时保留旧内容并记录明确状态。
- **B09-T09/T10 重排**：`POST /api/rerank` 只对已授权/已过滤候选做同 rubric 打分，预算/失败回原序，关闭或错误时明确 `applied:false`。
- **B09-T11/T12 提案**：Worker 从重复人工实体纠正生成 pending 提案；审批后 display-only 改名可应用（不改语义、不重评），语义变更显式返回 `requires_new_version`，不自动改词表。

### 验证（E `a85b870` / S `ccd7080`）
| 命令 | 结果 |
|---|---|
| E `make verify`（lint-ci + race + 73/73 frontend + build） | exit 0 |
| E `make test-browser` | 38/38 |
| E `tests/local-integration/run.sh`（真实 Worker/D1/R2 + Go） | 2/2 PASS（全生命周期 + 20 轮 A/B 与 in-flight 目标切换） |
| E `tests/local-integration/browser-e2e.sh`（真实 Go serve + Worker + Chrome） | 16/16 PASS |
| S `npm test` / `typecheck` / `deploy:dry-run` | 113/113 / exit 0 / exit 0 |
| Android `testDebugUnitTest` / `lintDebug` / `assembleDebug` / `compileDebugAndroidTestKotlin` | 55/55 / BUILD SUCCESSFUL |

新增回归：`internal/extension/service_test.go`（禁用/预算/越界/空结果/回退）、
`internal/processor/stages_test.go`（扩展成功与 blocked 路径）、
`internal/dashboard/reader_v2_test.go`（重排边界）、S `test/domain.test.ts`（实体状态/证据请求/提案生成与应用）。

真实浏览器端到端覆盖：真实服务注册 spec → 激活 v2 target → 新收藏经真实 scheduler 抓取/阅读/分类
→ 浏览器加载真实 Go 代理 → 显示与 Worker 一致的有效主题与第四主题折叠 → 人工 reject 落库并在刷新后保持。
付费模型调用 0；本地 mock 调用为抓取+阅读+分类各 1 次。



## 2026-09-21 第三轮最终门禁（含设备）

两仓测试代码 SHA：E `6abc76d` / S `e08d41f`。

| 命令 | 结果 |
|---|---|
| E `make verify` | exit 0 |
| E `make test-ablation` | exit 0 |
| E `make test-browser` | 38/38 |
| E `tests/local-integration/run.sh`（真实 Worker/D1/R2 + Go） | 2/2 PASS |
| E `tests/local-integration/browser-e2e.sh`（真实 Go + Worker + Chrome） | 16/16 PASS |
| S `npm test` / `typecheck` / `deploy:dry-run` | 113/113 / exit 0 / exit 0 |
| Android `testDebugUnitTest` / `lintDebug` / `assembleDebug` / `compileDebugAndroidTestKotlin` | 55/55 / BUILD SUCCESSFUL |
| Android `connectedDebugAndroidTest`（真实模拟器 plico-api26-x64，API 26） | **11/11 PASS** |

设备测试在真实模拟器上发现并修复了一个真实缺陷：多维区此前从 v1 词表渲染，
没有 content_functions/carriers/affordances；现改为加载 `/api/v2-taxonomy`
（S `e08d41f`）。

新增：SC26 问题级重用、B04-T06 有界分批、B08 生产导出（E `6abc76d`）。



## 2026-09-21 R2 复审修复轮（R2-01–R2-14）

两仓测试代码 SHA：E `1462fb4` / S `4632e86`（分支 `impl/jev-v2-fixes-20260921`）。

### R2-01 失败守卫不产生成功记录（S #28/#29，E #11 联动）
- Worker `classification.ts`：links 更新、run、decision、成功 operation 共享**同一事务内谓词**
  （lease/revision/input_revision/content_revision/policy/taxonomy/target/spec + 当前 target
  指针），并调整语句顺序使依赖写入在 job 完成前执行；守卫失败时四者全部 0 行，返回 409 且
  无任何成功历史。`idempotent` 对 operation 使用 `RETURNING` 判定，避免 INSERT 无 RETURNING
  时 `results` 为空被误判。
- 回归 `R2-01: a failed completion guard leaves no success run, decision or operation`：
  claim 后修改 source → 首次 409、同 key 重试仍 409、三张表 0 行、projection 未变、job 重新入队。

### R2-02 固定输入与目标身份（S #28/#29，E #12）
- claim 绑定 `content_revision`/`evidence_snapshot_id`/`evidence_hash` 并返回；complete 校验
  body 回显的这些身份与**事务内当前 target**，run 记录领取时 revision（迁移 0016）。
- 回归 `R2-02`：echo 旧 revision → `input_changed`；有效完成 run 的 content_revision == 领取值。

### R2-03 旧人工结果与新旧读写统一（S #29/#30，E #13）
- 旧 `links.curation` 一次性导入为 `legacy_unknown` override 层（revision 0）；自动基线改为
  `links.classification`（AI 结果），不再把人工解析后的 `link_selections_v2` 反充 automatic；
  真实旧端点 `PATCH /api/enrichment/jobs/:id/curation` 与 `PATCH /api/links/:id/curation`
  转成同一 override/事件日志；`classification: null` 对 v1 维度执行整维 reset 恢复真实自动值；
  `links.curation` 作为 v1 投影仅在人工结果存在时写入。
- 既有回归（curation/app-enrichment/classification 共 3 项）与新增行为共同验证。

### R2-04/R2-05 Android（S #31）
- `flush` 仅在服务端确认 `Applied` 后出队；`Queued`/`Network`/`Timeout` 保留动作、operation key、
  expected revision 与草稿，且不显示“已同步”。
- ViewModel 串行动作队列：快速连续编辑不再被 busy 丢弃；冲突重应用**重放原始逻辑动作**
  （reject/set_empty/reset/多标签增删）并使用原 operation key，而非 accept(first)。
- 回归：`V2CurationRepositoryTest` 新增离线/超时/混合 flush 与多字段意图测试；
  `V2CurationInstrumentedTest` 断言冲突后重新应用发送的是 `reject llm` 且 key 不变。
- 设备：`connectedDebugAndroidTest` 在 API 26 模拟器 11/11 PASS；unit 60/60、lint/build 成功。

### R2-06 真实刷新来源（E #12，S #28）
- Worker `refreshSource` 写 `refresh_epoch`/`refresh_requested_at` 并返回 epoch；enrichment claim
  返回该 epoch；新增 `POST /api/enrichment/jobs/:id/refresh-source/ack` 消费意图（成功/失败）。
- Go processor 在 `RefreshEpoch>0` 时**绕过 GetSource 与 legacy_saved** 直接 FetchSource，
  保存后 ack；失败保留旧可读内容并 ack failed。
- 回归 `TestRefreshIntentBypassesSourceCaches`：显式刷新 FetchSource 恰好一次；失败保旧内容。

### R2-07 补证据进入后续模型输入且不循环（E #15/#12，S #29/#30）
- claim 绑定 snapshot id；Go 通过 `GET /api/v2/links/:id/evidence?snapshot_id=N` 读取
  **同一快照**并构造结构化 `classify.Evidence`（保留全部 block、role、truncation），
  provider state 来自该结构；内容 hash 排除 `fetched_at`，相同内容不再制造新 revision。
- `CreateEvidenceRequest` 返回状态；非 pending（进行中/已完成）不再 fetch。
- 回归 `TestBoundEvidenceUsesTheStructuredSnapshot`、`TestEvidenceEscalationSkipsDecidedRequests`。

### R2-08 实体成功基线（E #15，S #29/#30）
- `computeEffective` 以 `entity_states` 成功结果为实体 automatic 基线再应用人工 override；
  entity stale 使用实体自身 content revision；not_run/failed 不清同源成功值。
- 回归 `R2-08`：实体基线可见、reject 移除、reset 恢复。

### R2-09 熔断恢复语义（E #11/#12）
- 只有模型阶段成功才 clear；Worker claim/空队列不再清除熔断；半开探测原子限量（`probing`），
  失败计数跨 poll 累加使退避增长（30s→1m→…）。
- 回归：`TestCircuitBreakerBackoffGrowsAcrossFailedProbes`、`TestCircuitBreakerProbeIsAtomic`。

### R2-10 显示名契约（S #30，E #12/#13）
- Go `TaxonomyTerm` 增加 `display_overridden`（可选），strict 解码与启动不再被真实字段击穿。
- 真实集成 `TestLocalWorkerDisplayRenameKeepsSemantics`：提案创建→批准→应用 rename→真实
  Worker→Go `GetV2Catalog`→`CompileSpec`，语义 hash 与 spec id 不变、label 改变。

### R2-11 随机 CI 与重排候选绑定（E #15/#16）
- 测试按候选身份（instructions 中的材料 ID）赋分，不再依赖 map 遍历顺序；
  生产 `RerankCandidates` 每个问题在 instructions 中明确绑定自己的候选 ID/文本，越界答案回退原序。
- 回归：`reader_v2_test.go` 捕获实际请求逐问核对绑定并重复 5 次稳定；`service_test.go` 越界答案回退。

### R2-12 统一幂等身份（S #29/#28/#30，E #11）
- run/override/decision/entity-state/evidence-request 均比较 operation key 的**归属收藏**与
  **规范 payload hash**；异 link/异 payload/异动作返回 `operation_conflict`(409)，
  DO NOTHING 不再掩盖异请求；decision 的 expected_revision 在新 operation 路径也校验。
- 回归：`R2-12`（同 key 异动作/异 link）、run 同 key 异 payload 冲突。

### R2-13 复用/分批安全与生产接入（E #12/#14）
- `PlanReuse` 比较 batch 语义；`EvaluateReusing` 从**实际序列化 state** 重算 evidence hash，
  跨 resolved model 不合并（partial + missing）；分批 usage 逐调用汇总，缺失标 missing；
  `Classify` 超过请求上限时自动分批；partial coverage 可决策但记录 `missing:*`。
- 生产接入为显式 opt-in（`CAIRN_PARTIAL_REUSE`）：processor 读取最近成功 run 与 spec，
  兼容时走 `ClassifyReusing`，否则回退完整评估。
- 回归：reuse/batch 单测 + `TestPartialReuseIsOptInAndFallsBackSafely`。

### R2-14 词条边界与语义规格升级（E #12，S #30）
- `taxonomy.Term` 增加 `Includes/Excludes`；`GetV2Catalog` 不再丢弃；`semanticDescription`
  纳入边界示例，问题与语义 hash 随 excludes 改变而改变。
- spec id 内容寻址（`classify-<hash12>`）：定义变化自动注册新不可变 spec，显示 label 改名不变；
  旧 spec 保留可重放，目标切换受控。
- 回归：excludes 改变 hash+id、label 改名不变、真实集成 rename 后 id/hash 不变。

### 门禁（本轮，两仓 SHA 见上）
| 命令 | 结果 |
|---|---|
| E `make verify` | exit 0 |
| E `make test-ablation` | exit 0 |
| E `make test-browser` | 38/38 |
| E `tests/local-integration/run.sh`（真实 Worker/D1/R2 + Go） | 3/3 PASS |
| E `tests/local-integration/browser-e2e.sh`（真实 Go + Worker + Chrome） | 16/16 PASS |
| S `npm test` / `typecheck` / `deploy:dry-run` | 117/117 / exit 0 / exit 0 |
| Android `testDebugUnitTest` / `lintDebug` / `assembleDebug` / `compileDebugAndroidTestKotlin` | 60/60 / BUILD SUCCESSFUL |
| Android `connectedDebugAndroidTest`（API 26 模拟器） | 11/11 PASS |

模型调用：全部测试 0 付费调用；真实集成中仅本地合同 mock（抓取/阅读/分类各 1 次）。

