# R3-04 single-choice order and R3-11 build evidence · 2026-09-22

Tested code: Share `76b1705d764416e95a93fed87e38966e30c21772`; Enricher `e85632028ad70aa5adb64ba6d9fd5c4f4798c94b`. This report is a later documentation-only commit. R3 baseline and original requirements remain open pending the complete matrix.

## R3-04

Before the fix, all nine new Go reaccept/reject vectors failed; the Worker shared-vector test stopped at the first failure. The resolver kept each term's first appearance order while updating its latest action. It now retains actual action history for single-valued fields (form, use, carriers). Rejecting the active value clears it instead of reviving a prior accept. Per-term reset removes only that term's history; field reset clears the full history. Multi-value ordering remains unchanged.

The byte-identical Go/Worker fixture contains 26 vectors (11 prior + 15 added), including A→B→A, reject-active, reject/reaccept, and tag/field reset for all three single-value fields. Three additional real D1 route tests persist each operation and read the effective selection independently.

| Command | Result / scope |
|---|---|
| `make verify` | exit 0; Go vet/lint/race, 73 frontend checks, build |
| Worker `npm test && npm run typecheck` | exit 0; 132/132 tests + tsc |
| `go test ./internal/classify` and Worker `npx vitest run test/domain.test.ts` | exit 0 after the final six reset vectors; 30 Worker domain tests |
| `bash tests/local-integration/browser-e2e.sh` | exit 0; 21/21 checks; real Chrome + Go server + Worker + D1/R2 |

The browser performs normal bookmark retrieval/reading/classification, rejects a topic, selects carrier A→B→A, reloads and opens another browser page. Both pages show A. External paid models are local fixtures; no live-model quality or Android-device coverage is claimed. The first new API test used an obsolete carrier name and returned 400; corrected to actual taxonomy IDs `single` and `external_article` before the passing full run. No assertion or validation was weakened.

## R3-11 production build boundary

Remote CI exposed production `cmd/export.go` importing `experiments/classification`, absent from the production Docker context. Commit `ad3a477fb98a999581e538650e99fcb43ec06557` moved reusable scoring/export code into `internal/evaluation`; the experimental CLI still runs and `test-ablation` still covers its moved tests. Production dependency enumeration contains no experiments import. Offline scorer output before/after is byte-identical.

[Enricher run 35701205989](https://github.com/Alpenl/cairn-x-enricher/actions/runs/35701205989) passed Test and lint and Build container (amd64/arm64) at that exact SHA. [Share run 35700693958](https://github.com/Alpenl/cairn-share/actions/runs/35700693958) passed Worker and Android at `3c59f547d0c3a1acf5b5959dd16383709d6cf33d`. These CI results precede the R3-04 commits above; their new CI results must be checked separately. Local Docker build was blocked by `proxy.golang.org` DNS network timeout; not counted as passed.

Logs: [curation evidence](logs/20260922-curation/). No production deployment, migration, merge or live API call was performed. This is implementation evidence, not independent acceptance of all 126 tasks.
