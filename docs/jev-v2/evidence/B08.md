> 2026-09-23 载体边界对照：[生产词表与真实两臂训练实验](B08-20260923-carriers.md)。S `f7ed68e` / E `d4d0e3b`；生产定义 v2，真实 API 与实验词表一致，仅载体问题改变。两臂各 42 次，载体 F1 0.8974 → 1，潜在用途 F1 略降；7 独立组，train-only，未晋升。210 Worker、完整 Go 门禁与 12 个实际服务场景通过。新增 84 次、累计 211 次，完整原范围保留。

> 2026-09-23 客观/个人边界修复：[新候选、版本化 policy 与真实 Worker 写入守卫](B04-20260923-personal-intent.md)。S `9571445` / E `8dced00`；默认 v3 不再从客观 raw 产出 contra，Worker 拒绝旧/新自动写入口绕过，显式人工值和历史审计保留。210 Worker、完整 Go 门禁与 10 个实际服务场景通过；本批零付费，累计 127 次。新 spec 质量尚未验证，完整原范围保留。

> 2026-09-23 最新 B08 实验：[来源角色合同与真实训练负结果](B08-20260923-source-roles.md)。E `315f7d9`；42 对请求只改共享角色描述。召回 89.05% → 87.14%、错误接受 5.08% → 6.63%，载体 F1 未改善，输入成本上升；工程门禁/9 个实际服务/源码 CI 通过，但质量未验收。新增 42 次、累计 127 次；train-only，参考/门槛不变。后续备注隔离和载体词条边界独立验证，完整原范围保留。

> 2026-09-23 最新 B08 受控实验：[实际输入字段名修复与 42 条训练对比](B08-20260923-state-fields.md)。E `97b9e32`；实际请求验证仅两个字段名变化，参考和 policy 不变。召回上升，但载体/潜在用途 F1 下降、完整字段覆盖下降，未晋升、未通过完整质量验收。本地完整门禁、9 个实际服务场景和源码 CI 通过。新增 42 次，累计 85 次；dev/holdout 未用于模型、拟合或质量评分。后续来源角色和备注隔离独立验证，完整原范围保留。

> 2026-09-23 最新训练结果：[六维生产 policy 拟合](B08-20260923-policy.md)。336 个候选均未达冻结条件，没有 selected policy；本批 0 付费，训练仅 7 个独立组，不构成质量通过。模型/spec/实际材料身份校验、risk/config 和候选全记录已接入，完整 B08 仍在推进。

> 2026-09-23 最新工具修复：[门槛校验、holdout 范围与消融可比性](B08-20260923-gate.md)。保留历史报告，完整真实质量验收仍未完成。

> 2026-09-22 当前工程进展：[生产存读后的问题级复用、调用来源与历史输入身份](R3-20260922-reuse.md)。受测 S `dcfd612` / E `d606002`；Worker 148 项、Go 完整检查、6 个真实跨仓场景通过，本批 0 次付费调用。自动参考训练诊断与整体验收仍按各自范围记录，下文历史证据保留。

> 2026-09-22 当前进展：[自动参考基准、真实训练评估与零网络恢复](B08-20260922-automatic-reference.md)。所有者已授权无需人工标注；240 条参考已先行冻结，42 条训练真实调用完成，holdout 尚未调用，质量仍未验收。下文按原日期保留，旧人工 gold/live 授权阻塞与 runner 未启用说明不再代表当前状态。

# B08 证据：人工标注评估、逐项消融与校准晋升门禁

报告日期：2026-09-20
Batch / Issue：B08 / [E #14](https://github.com/Alpenl/cairn-x-enricher/issues/14)
Covered task IDs / scenario IDs：B08-T02、T05、T06、T07、T08、T09、T13、T14（工程部分）；SC06、SC07、SC08、SC09、SC22、SC25、SC26、SC29、SC30
Implementation branch：`impl/jev-v2-b02-runtime-safety`
Companion SHA：S B01/B03/B05 分支
设计/spec 版本：`docs/jev-v2/08-evaluation.md`

## 变更

新增独立离线包 `experiments/classification`（不挤入生产包）：

- **B08-T02 数据 schema/校验**（`dataset.go`）：`Sample` 含 sample_id、source_hash、revision、
  language/carrier/length/completeness、group_id、检索相关性、多标签 gold（含
  `not_applicable`/`unknown`）、`Provenance`、ambiguity、第二标注者标记。
  `Validate` 拒绝重复 sample_id、缺 source_hash、缺 provenance，并拒绝 `legacy_unknown`
  携带 gold（旧隐式 reviewed 不是自动正样本）。`NormalizeLanguage` 归一化中英混合分层。
- **B08-T05 分组切分**（`SplitByGroup`）：先按 thread/近重复 group 聚合，再以 seed 的稳定
  哈希分配 train/dev/test，保证同组不跨集合。
- **B08-T06 纯离线 scorer**（`score.go`）：每维 precision/recall/F1、micro/macro、
  accepted error、coverage/abstention、review fields/条、按语言/长度/载体分层计数、
  support 与缺失 gold 计数。零分母显式规则（0/0=0，绝不等于满分）；多标签不使用单类 accuracy。
- **B08-T07 校准**：从保存的 topic 分布计算 Brier 与 ECE（10 分箱）+ 非空箱数，披露分箱限制；
  分布差异不会被折成单一数字。
- **B08-T08 阈值重放与搜索**：`Replay` 从存储概率重算 accepted（零模型调用）；
  `SearchThresholds` 仅在 train 上拟合并遵守 coverage floor。
- **B08-T09 消融比较**：`CompareAblations` 对基线缺失的维度标 `Comparable=false`，不做异任务相减。
- **B08-T13 晋升门禁**：`GateThresholds` 冻结风险/coverage/负担/成本门槛；accepted error 作为
  安全指标独立于 recall 检查；样本不足 → `inconclusive` 且不晋升；
  `DetectModelDrift` 对 alias 漂移要求重新评估。
- **B08-T14 报告与 CLI**：`experiments/classification/main` 提供 `-dataset`/`-gate`/`-dry-run`；
  `-dry-run` 输出计划且 `model_calls: 0`；`-live` 需要显式正数 `-max-calls` 预算且本构建
  明确拒绝（未启用 live runner），环境有 key 不等于授权。
- **合成 fixture + golden vector**：`testdata/synthetic-dataset.json` 与两个 golden 测试固定
  报告形状、哈希稳定性与可重复性。

## 回归与门禁

| 命令 | 结果 |
|---|---|
| `make verify`（lint + lint-ci + test + test-frontend + build） | 退出码 0 |
| `make test-ablation` | 退出码 0 |
| `go test ./experiments/classification/` | 通过（约 16 个测试） |
| `golangci-lint` | 0 issues |
| `go run ./experiments/classification/main -dataset testdata/synthetic-dataset.json` | 稳定 JSON，`promote:false`+`inconclusive:true` |

关键断言：零预测/零正例不是满分；全弃权不能换漂亮 precision；合法 none 计为 support 且非
错误；micro 与 macro 在不均衡样本下不同；过度自信分布产生大 ECE/Brier；重复 sample 被拒；
legacy_unknown 不能带 gold；分组切分不泄漏；阈值重放零调用；消融对不可比维度标记；
安全失败即使统计良好也阻止晋升；缺 gold 标记 inconclusive；无真值不输出 accuracy。

- 模型调用计数：0（全部离线；live 路径被显式拒绝）。
- 数据隐私：fixture 为合成数据，不含私人原文/备注/实体/标注。

## B08-T01 / B08-T03 / B08-T04 / B08-T10 / B08-T11 / B08-T12

- **B08-T01 基线/授权准备**：部分。两仓基线 SHA 已记录（S `5cf3b0d…`、E `cb13d0e…`），
  v1 schema/policy 已冻结（`classify.PolicyVersion` 与 taxonomy 版本）。**没有真实私有 gold
  或授权 raw 记录**，未用新模型补造旧输出。
- **B08-T03 分层采样**：schema 与分层计数已支持（语言/长度/载体）；实际 200–300 条人工采样
  为 `blocked_external`（无 gold 与授权）。
- **B08-T04 标注指南/复核**：`second_annotator` 与 `ambiguous` 字段已支持；实际指南与独立
  复核为 `blocked_external`。
- **B08-T10 找回评估**：`Sample.Relevance`/`RetrievalQuery` 字段已具备；实际固定候选与
  Recall@K/nDCG 为 `blocked_external`（无授权检索 gold）。
- **B08-T11 成本/性能**：未实现 runner；标注 `blocked_external`（无 live 授权与样本）。
- **B08-T12 Live runner**：预算/dry-run/拒绝无授权已实现（CLI）；真实 live 为
  `blocked_external`。

## 限制和审批

- `quality_verified=false`：**无人工 gold，未验证真实模型质量**；不得以本文件声称准确率或晋升。
- 未完成真实样本数：gold 缺失，无法给出有效样本数与指标区间。
- engineering_done：是（T02、T05–T09、T13、T14 有代码与测试）
- review_accepted / merged / deployment_authorized：否
- 实际生产操作：无；未调用模型、未远端迁移、未部署。
- 后续审查要求：补人工 gold、检索 gold 与 live 授权后再评估晋升；勿以本文件自动关闭 E #14。


## 2026-09-21 F01–F14 修复轮证据

实现：Enricher `impl/jev-v2-fixes-20260921` 代码 SHA `faad44c`（PR #17，上游固定审查 SHA `4031200984d819b59baea593025621a04deeb984`）；
Worker companion `4c03094`（PR Alpenl/cairn-share#32，上游固定审查 SHA `8f98ac0a9d6e5257840bd518cc5960464b74a98c`）。
本轮没有手工 seed 目标历史表：`tests/local-integration/run.sh` 从新收藏走真实链路生成记录。

| 命令 | 结果 |
|---|---|
| `make verify`（lint-ci + `go test -race` + 73/73 frontend checks + build） | exit 0 |
| `make test-ablation` | exit 0 |
| `make test-browser` | 29/29，exit 0 |
| `tests/local-integration/run.sh`（真实 Worker/D1/R2 + 真实 Go client/classifier） | PASS |
| Worker `npm test` / `npm run typecheck` / `npm run deploy:dry-run` | 107/107 / exit 0 / exit 0 |

真实组合证据：`tests/local-integration/run.sh` 启动 `wrangler dev`（真实 migrations、本地 D1/R2），
以真实 `cairn.Client` + 真实 `classify.Client` 完成 建收藏→SaveSource→SubmitEvidence→PutQuestionSpec→
激活 v2 target→handshake→claim→Evaluate（仅付费模型端点被官方合同 mock 替换）→complete→GetRuns→
DecodeStoredJudgments→Replay→ApplyV2Override→SubmitDecision→GetV2Selection，并断言人工 reject 在
replay 后仍生效。模型调用计数：付费调用 0；本地 mock 调用 1；replay 0。


### F06/F14 联动（本批）
评估工具消费的 run 现在可真实重放：`classification_runs.policy` 保存历史 policy 原文，
`GetRuns`/`GetQuestionSpec` 返回结构化 JSON；`cmd/.../replay.go` 用真实 spec + 历史 policy 解码，
最后一条 partial/failed 时显式选择最近一条 succeeded/complete，找不到则报错（
`selectReplayRun`）。`--commit` 在 `--authorize-write` 前保持 dry-run，授权后追加 decision 而不新建
模型 run。usage 缺失标 missing，不再伪造为 0。真实 gold/live 质量仍 blocked_external。
状态：工具 engineering_done；quality_verified=false。



## 2026-09-21 第三轮：生产数据接入（B08-T08/T14）

实现 SHA：E `6abc76d`。

- `experiments/classification/export.go`：`ExportDataset` 从 Worker 读取显式 ID 的
  最新 succeeded/complete run，加载真实 spec 与历史 policy，纯计算
  `Decide` 得到预测；样本 provenance 标为 `synthetic`（机器输出），**不伪造 gold**；
  预测携带真实 `spec_id/spec_hash/model/policy_version` 与 topic 概率分布。
- `cmd/cairn-x-enricher export-dataset --ids ... --out ...`：只读 Worker、零模型调用、
  ID 显式且有界（≤500）。
- 边界：无显式 ID、超界、缺历史 policy、最后一条 partial/failed 都有测试
  （`export_test.go`）；导出数据集经 `Score` 得到 `inconclusive`（`SamplesWithGold=0`）。
- 真实组合：`TestLocalWorkerFullLifecycle` 第 8 步对真实 Worker 导出并断言
  `Model=jev-pinned-local`、`PolicyVersion=jev-policy-v2`、无 gold、inconclusive。

真实质量仍 blocked_external（无人工 gold / live 授权），本轮只完成可独立交付的
生产导出与评分链路。

