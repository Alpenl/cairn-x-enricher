# B02 证据：显式确认、独立阅读契约与运行时安全

报告日期：2026-09-20
Batch / Issue：B02 / [E #11](https://github.com/Alpenl/cairn-x-enricher/issues/11)
Covered task IDs / scenario IDs：B02-T01、T02、T03、T04、T06、T08（幂等键部分）、T09、T10；SC03、SC04、SC10、SC11、SC25（工程部分）
Implementation PRs：本分支 `impl/jev-v2-b02-runtime-safety`（提交见下）
Base / Head / Companion full SHA：E base `cb13d0e543e54f18baf6e151630d728b3175e792`；E head `ea87815adb1ff4aec41f639b8bc08a06de25c93a`；S companion `5cf3b0d45c8fbae07d0fb5be1772e7d747c69b4b`（未改动）
设计/spec/model/policy/dataset版本：`docs/jev-v2/02-runtime-safety.md`（设计 PR #3 提交 `62d29cfea0553e49ce1e5edece7dfcaba28e9c14`）

## 已验证的具体行为

- **B02-T01/T02 显式确认**：`internal/dashboard/reader.js`、`reader.html`。原因/状态与标签分别维护 dirty；只有实际触碰标签或点击“确认这些标签”才在 PATCH 中发送 `classification`。回归：`internal/dashboard/frontend-check.mjs` 新增 why-only / status-only / why+status 三种保存均断言 PATCH 不含 `classification`；显式取消勾选与显式确认两条路径断言必须携带。提交 `c2b69e8`。
- **B02-T03/T04 独立 ReadingResult**：`internal/enrich/responses.go`（`readingSchema`）、`internal/enrich/source.go`（`Transform` → `decodeReading` → `validateReading`）。阅读 schema 不再从 enrichment schema 删字段拼装；模型只输出 title/language/translation/summary，原文/链接/图片由程序从已存快照注入。缺失字段、尾随内容、非 JSON、未知键、尝试重写原文均显式失败。提交 `c2b69e8`。
- **B02-T06 typed 错误分类**：`internal/enrich/errors.go`。401/403/402 → configuration，400/422/415 → contract，408/429/5xx → transient，409 → stale，其余 unknown；`retryableModelError` 仅对 transient 就地重试；`isContractFailure`（`cmd/.../main.go`）据类暂停组件，冲突/过期不降 readiness。提交 `ea87815`。
- **B02-T08 幂等**：`internal/enrich/responses.go` 的 `modelIdempotencyKey`。阅读请求以来源指纹而非 job attempt 为幂等键，完成响应丢失后的重试复用服务端结果，不重复付费推断。提交 `ea87815`。
- **B02-T09 角色化配置**：`internal/config/config.go` 的 `Role` / `LoadFor`。`classify` 只校验 Worker + Typesafe，不再要求未使用的 Grok 配置；enrich/serve 仍要求阅读凭据；所有角色都要求 Worker token。`cmd/cairn-x-enricher/classify.go` 改用 `RoleClassify`。提交 `ea87815`。
- **B02-T10 source-first**：既有 `internal/processor/stages_test.go` 回归（source 先存、阅读失败重试不重抓、分类失败不触发 X Search、阅读不写 classification）在本次改动后仍通过。

## 回归与门禁

- 修复前失败复现：`node internal/dashboard/frontend-check.mjs internal/dashboard` 在改动前对 why-only 保存报 `FAIL why-only save omits classification`（PATCH 携带 `classification`）。
- 修复后相同场景：`69/69 checks passed`。
- Unit / contract / 前端覆盖：`go test -race ./...`（全绿）、`node frontend-check.mjs`（69/69）、`go test ./experiments/...`（离线重放，无模型调用）。
- 命令/退出码：`make verify`（lint + lint-ci + test + test-frontend + build）退出码 0；`make test-ablation` 退出码 0。
- CI URL：本地执行，未推送远端；无付费调用。
- X Search/Jev 调用计数：0（全部 mock/离线）。
- 禁网 policy replay：本批无 policy 层改动，未涉及。
- 迁移及应用/目标回滚：无 schema 迁移；阅读/确认改动回滚保留原文与人工值。

## 限制和审批

- 私有数据授权位置策略：未使用私人原文、备注或标注；测试均为合成本地 fixture。
- 未运行测试及 blocked_external：
  - B02-T05（B01 capability/target 握手）+ T07（持久化退避层统一）+ T08 的 Worker 侧幂等合同 + T11 真实浏览器执行，均 `blocked_external`，依赖 [S #28](https://github.com/Alpenl/cairn-share/issues/28) B01 的已验证目标/幂等实现 SHA。本批未伪造其完成。
  - 真实浏览器断言（T11）需要可用实例；本次仅以 Node DOM shim 覆盖交互逻辑，不冒充浏览器执行。
- 已实现 feature flags 与默认状态：无新 flag；行为修复默认生效。
- engineering_done：部分（T01–T04、T06、T08 幂等键、T09、T10）
- quality_verified：否（真实模型/浏览器/Worker 联调未运行）
- review_accepted：否
- merged：否
- deployment_authorized：否
- 实际生产操作：无
- 风险/设计偏离及依据：无设计偏离；`retryableModelStatus` 保留以避免破坏既有测试，分类映射为其超集并新增 stale/configuration 语义。
- 后续审查要求：待 B01 联调后补 T05/T07/T08 Worker 侧与 T11 浏览器证据；勿以本文件勾选整个 B02 Issue。
