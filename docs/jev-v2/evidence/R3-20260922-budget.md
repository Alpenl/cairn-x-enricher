# R3-05 serialized input budget · 2026-09-22

Tested Enricher `ad6d80ba686d456575c09e362ddd9f3973622f2a` with Share `5c6b28d58aa550794308dfb93a7e43041c8dd537`. Maps to R3-05 / F14 / B04 input budget / B08 bounded evaluation / SC18,30. Full R/B/SC acceptance remains pending.

Structured snapshots now pass through the same preparation as plain text. Defaults are 12,000 text runes, 12 context blocks, 48 KiB serialized state and 128 KiB entire HTTP request. Rune counts are not represented as provider token estimates. Byte budgets include JSON escaping, block metadata, instructions and criteria. Context is removed from the end before primary text is further shortened. Snapshot objects and archived bytes remain unchanged. Existing upstream partial/truncated coverage is preserved, and dropping material exactly at the character limit is reported as truncation.

A question set that exceeds the total request limit is rejected before HTTP with a contract error; question definitions are not silently shortened. The total request gate covers full evaluation, subset reuse and extension calls. Provider state remains an object and questions remain the official typed map; no vendor API fields were invented.

## Verification

- Before/after: the new actual-HTTP tests fail with the previous `jev.go` and pass after the fix. Cases include structured long primary, many context blocks, oversized URL metadata and excessive criteria. The archive object is compared before/after. Separate cases preserve upstream coverage and detect omitted context at an exact rune boundary.
- `make verify && make test-ablation`: exit 0 (vet/lint/race, frontend 73/73, build, experiment/evaluation regression).
- `bash tests/local-integration/run.sh`: exit 0, four real Go/Worker/D1/R2 scenarios. The checkpoint scenario now archives a long synthetic source and runs the normal processor/classifier with a 200-rune / 1,000-byte state / 16-KiB request test budget.
- Actual observed archive response: **45,435 bytes**, unchanged byte-for-byte after inference. Actual provider request: **2,057 bytes**, state **613 bytes**, text **200 runes**, 0 context blocks; truncation recorded. Seven invalid bound-read cases still cause zero model calls and zero runs; healthy recovery creates one model call and one run.

[Logs](logs/20260922-budget/). Models are local fixtures. This proves engineering bounds on actual HTTP bytes and retained source, not live semantic quality. The earlier R3-02 code/report CI succeeded at Share `5c6b28d58aa550794308dfb93a7e43041c8dd537` ([run 35703536066](https://github.com/Alpenl/cairn-share/actions/runs/35703536066)) and Enricher `27e956677f5e4111758557be6eec3cd7b8c286b8` ([run 35703538230](https://github.com/Alpenl/cairn-x-enricher/actions/runs/35703538230)); this budget commit requires its own CI check. No production operation was performed.
