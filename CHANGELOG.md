# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/)。

## [Unreleased]

### 可靠性

- `/readyz` 现在反映真实可服务状态：启动前置检查完成前返回 `503`，并带上 `ready_reason` 和 `unhealthy_since`；`/healthz` 保持只表示进程存活，上游不可达时不再触发重启循环。
- 新增启动期模型契约自检（canary）：端点、模型名或密钥配错、或上游不再支持 strict JSON Schema 时立即失败退出，而不是静默耗尽每条收藏的重试次数。
- 明确的配置/契约错误（`400/401/403/404`）会把服务标记为降级，需要一次成功的启动级恢复才能清除。
- 关停时先排空已领取的队列任务（受 `SHUTDOWN_TIMEOUT` 约束），避免任务被丢弃并浪费 lease 与尝试次数。
- 模型重试加入 ±25% 抖动，避免多实例在共同上游故障后同步重试；慢请求不再叠加第二次长时间等待。

### 性能

- 词表和 JSON Schema 只渲染一次并在进程内复用，不再在每个请求中重复 JSON 编码。
- 词表在页面侧按 TTL 缓存，上游短暂不可用时回退到上一个可用版本。
- 后台聚合改为缓存加单次 list 调用（原来每次轮询 3 次上游请求）。

### 安全与健壮性

- 图片代理在 `Content-Length` 存在时按字节数校验上游响应，截断的图片会被记录而不是被浏览器静默接受。
- 结构化模型输出的解码加上大小上限，超长响应不会造成无界内存分配。
- 人工队列容量改用原子计数，并在入队时就加上上限检查。
- 前端错误映射补齐后端错误码，未知错误码直接展示而不再被泛化文案掩盖。

### 工程

- 不再跟踪 `dist/` 和 `docker-build/` 构建产物，清理已提交的归档与一次性分析结果。
- 根命令支持 `--version`。
- 测试覆盖从 68.4% 提升到 78.7%，补齐词表渲染、健康状态机、契约自检、重试抖动、缓存与错误映射等原先未覆盖的路径。

## [0.5.0]

- 归档收藏管理讨论的完整正文、结构化消息和原始 DOCX 手册。
- 通过 Worker 的统一词表生成主题、形态、用途、实体和收藏用途建议；程序处理别名、未知标签、数量上限与待确认状态。
- 增加独立于处理队列的人工收藏原因和整理状态，人工分类在重新处理后继续保留。
- 支持组合筛选、原文/原因/实体关键词搜索和 Markdown 导出，阅读页保留筛选导航上下文。
- 修复连续搜索的请求竞争、窄屏搜索框溢出及阅读页处理按钮一直禁用的问题。
- 需要配套 Worker 的迁移 0007 和新内部接口，两端配套升级；不自动回填历史收藏。

容器：`ghcr.io/alpenl/cairn-x-enricher:0.5.0`

## [0.4.1]

- 修复后台页在仍有失败/耗尽收藏时显示“一切正常”的状态误判；后台状态现在统一聚合最新巡检、最近实际处理批次和失败收藏计数。
- 模型端临时 `408/429/5xx` 错误会在单次队列 attempt 内短重试；完整线程读取慢失败时降级为只读原帖的结构化请求。
- 已有 `original_text` 的失败/耗尽记录会直接进入标题、语言、译文和摘要生成，避免重新触发不稳定的 X Search 抓取。
- 后台失败列表新增“粘贴原文”入口，可手动提交原帖正文后继续生成完整增强结果。
- 首页后台入口图标和重试按钮样式改为更明确的后台/重试语义。

容器：`ghcr.io/alpenl/cairn-x-enricher:0.4.1`

## [0.4.0]

- 处理台重做为内容优先的收藏首页：最近四条配图展示，其余按时间分组成三列速览，滚动到底自动加载更早的收藏。
- 列表不再展示状态标签、编号、作者和队列计数；尚未处理的收藏只显示原链接和一句说明，处理完成后自动换成标题与摘要。
- 搜索合并进顶栏，输入即搜标题、备注、摘要和译文并高亮命中处，搜索词写入地址栏可直接分享。
- 阅读页只保留内容：日期、标题、手动备注、摘要、图片和中文全文，原文默认收起，底部可直接跳到下一条。
- 新增 `/backstage` 后台页，集中展示服务状态和需要人工重试的收藏；首页仅在存在失败收藏时显示一个提示点。
- 列表单页上限从 20 条提高到 60 条，首页一次读取 40 条。

容器：`ghcr.io/alpenl/cairn-x-enricher:0.4.0`

## [0.3.0]

- 将处理台升级为“收藏列表 + 独立阅读页”，列表适合筛选和批量操作，阅读页展示完整内容。
- 模型严格生成约 20 个简体中文字符的主标题；原有手动备注作为独立信息完整保留。
- 同时保存原始语言全文、原始语言标识、完整简体中文译文、中文摘要和内容相关链接。
- 识别 X 原帖及相关评论中的图片，将允许的 `pbs.twimg.com/media` 内容复制到 Cloudflare R2，再经 NAS 同源代理安全展示。
- 管理页展示 Cloudflare 中全部收藏；非 X 收藏归入“其他”且不会触发 X/LLM 处理。
- 列表页和阅读页都展示当前运行版本与提交号，并针对桌面和移动端优化阅读布局。

容器：`ghcr.io/alpenl/cairn-x-enricher:0.3.0`

## [0.2.0]

- 将状态页升级为中文 X 收藏处理台，展示收藏备注、原链接、处理状态、总结、相关链接和错误。
- 支持搜索、状态筛选、分页、完整原文展开，以及单条或最多 10 条批量立即处理/重新处理。
- 增加同源管理 API 和有界人工处理队列；人工任务与定时任务共用全局并发限制。
- 对接 Cloudflare 收藏列表、详情和按 ID 原子领取接口，浏览器不接触 Worker 或模型凭据。

容器：`ghcr.io/alpenl/cairn-x-enricher:0.2.0`

## [0.1.1]

- 增加内嵌的只读服务状态页，展示健康状态、最近批次统计、构建版本和错误摘要。
- 增加根页面安全响应头、精确路由和回归测试。
- 增加 Momax NAS 专用 Compose，通过 `8088` 提供局域网状态页且仅使用 Actions 发布镜像。

容器：`ghcr.io/alpenl/cairn-x-enricher:0.1.1`

## [0.1.0]

首个可运行版本：

- 新增定时和单次两种运行模式，从 Cairn Share Worker 原子领取 X 收藏。
- 使用 CloudWeGo Eino 工作流和 xAI Responses `x_search` 生成原文、简短总结及相关链接。
- 增加严格 JSON、搜索证据和 URL 校验，失败由 Worker 做有界退避重试。
- 提供非 root、无 shell Docker 镜像以及 `amd64`/`arm64` GHCR 发布。
- 提供 health/status 接口、结构化日志、race test、lint 和完整 GitHub Actions Release 流程。

容器：`ghcr.io/alpenl/cairn-x-enricher:0.1.0`
